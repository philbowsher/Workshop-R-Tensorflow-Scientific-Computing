# get number of bins passed by R Shiny server
def testMethod(bins): 
  string = "I came from a Python Function" 
# convert int to string to concat
  noOfBins = str(bins) 
  return "You have selected " +noOfBins+ " bins and " +string
